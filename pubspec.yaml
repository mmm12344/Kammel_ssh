name: terminal_agent
description: "KAMMEL SSH — gestor de conexiones SSH y explorador remoto para agentes IA."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 2.10.3+75

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  # Localized strings for the built-in Material widgets (text-selection menus,
  # dialogs…) so they follow the language picked in Ajustes → Idioma.
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  xterm: ^4.0.0
  dartssh2: ^2.17.1
  path_provider: ^2.1.5
  shared_preferences: ^2.5.5
  provider: ^6.1.5+1
  # Biometric / device-credential unlock for the optional app lock (see
  # lib/services/app_lock.dart). Requires MainActivity to be a
  # FlutterFragmentActivity on Android.
  local_auth: ^2.3.0
  local_auth_android: ^1.0.46
  re_editor: ^0.9.0
  # Syntax-highlighting grammars + token themes used by the code editor (see
  # lib/theme/code_highlight.dart). Pulled in transitively by re_editor, but we
  # import it directly so it must be a declared dependency.
  re_highlight: ^0.0.3
  uuid: ^4.5.3
  google_fonts: ^8.1.0
  flutter_markdown_plus: ^1.0.7
  flutter_secure_storage: ^10.3.1
  # Runtime request of the legacy storage permission so the local distro can
  # bind the phone's shared storage (see DistroService.launch).
  permission_handler: ^12.0.1
  # Open URLs detected in the terminal in the external browser.
  url_launcher: ^6.3.2
  pdfrx: ^2.2.24
  # SVG rendering for the embedded image viewer (raster formats use
  # Image.memory directly).
  flutter_svg: ^2.2.0
  http: ^1.6.0
  # Pinned to the 9.x line: 10.x requires win32 ^6.0.1, which conflicts with
  # file_picker's win32 ^5.9.0 and breaks the Linux build. Only fromPlatform()
  # is used, so the older major is functionally equivalent here.
  package_info_plus: ^9.0.1
  open_filex: ^4.7.0
  media_kit: ^1.2.6
  media_kit_video: ^2.0.1
  media_kit_libs_video: ^1.0.7
  pasteboard: ^0.5.0
  # Native file picker for the terminal's "adjuntar" quick key (attach any
  # document/image and insert its path into a TUI agent's prompt).
  file_picker: ^8.1.6
  # Voice dictation in the prompt composer (Android SpeechRecognizer). Android
  # only in practice; the mic button is hidden on other platforms.
  speech_to_text: ^7.0.0
  # Device SSH key generation (lib/services/device_key.dart): pinenacl provides
  # ed25519 keygen and crypto the SHA-256 fingerprint. Both already come in
  # transitively via dartssh2, but we import them directly so they must be
  # declared (same rationale as re_highlight above).
  pinenacl: ^0.6.0
  crypto: ^3.0.0
  window_manager: ^0.5.2

# Use a locally-vendored, patched copy of xterm (third_party/xterm) so the
# terminal keyboard improvements survive `flutter pub get` on any machine:
#  - normal keyboard with suggestions / autocorrect / voice dictation (the
#    upstream package hardcodes an "incognito" keyboard with all of that off);
#  - inline image paste via the soft keyboard (Gboard "insert content"),
#    exposed through TerminalView.onInsertContent.
# See third_party/xterm for the patched sources.
dependency_overrides:
  xterm:
    path: third_party/xterm
  # Patched fork of dartssh2 2.17.1. Two fixes, both documented inline with a
  # "KAMMEL patch" comment: the channel receive window is never replenished once
  # exhausted (any transfer through a tunnel deadlocks at ~6 MB), and host key
  # verification only exposes an MD5 digest, too weak to pin on — the fork adds
  # a handler that receives the raw key blob so we can use SHA256.
  dartssh2:
    path: third_party/dartssh2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_launcher_icons: ^0.14.4

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

flutter_launcher_icons:
  android: true
  # Generated from public/icon/ugu.png, which ships as opaque RGB with black
  # corners; ugu_icon.png is the same card art with the rounded corners cut to
  # real transparency so the legacy square icon isn't boxed in black.
  image_path: "public/icon/ugu_icon.png"
  adaptive_icon_background: "#EE2A25"
  # The adaptive foreground is the camel + SSH badge alone, already padded to
  # sit inside the 66% safe zone, so no launcher mask (circle/squircle/
  # teardrop) can clip it. Using the full card art here is what made the icon
  # look zoomed in and cut off.
  adaptive_icon_foreground: "public/icon/ugu_foreground.png"
  adaptive_icon_foreground_inset: 0
  min_sdk_android: 21

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    # Camel silhouette (white on transparent) — tinted at draw time.
    - assets/images/kammel_logo.png
    # Real agent marks, keyed by the same ids the detector and the Android
    # notification badges use (see AlertNotifier.agentBadge). Drawn as-is:
    # these are brand marks, so unlike the logo above they are never tinted.
    - assets/agents/

  fonts:
    - family: CascadiaCode
      fonts:
        - asset: assets/fonts/CascadiaCode-Regular.ttf
        - asset: assets/fonts/CascadiaCode-Bold.ttf
          weight: 700
        - asset: assets/fonts/CascadiaCode-Italic.ttf
          style: italic
        - asset: assets/fonts/CascadiaCode-BoldItalic.ttf
          weight: 700
          style: italic

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
